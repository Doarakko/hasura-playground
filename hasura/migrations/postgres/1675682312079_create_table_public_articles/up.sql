CREATE TABLE "public"."articles" ("id" serial NOT NULL, "user_id" integer NOT NULL, "title" text NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("user_id") REFERENCES "public"."users"("id") ON UPDATE cascade ON DELETE cascade);
